import { FilterBySearchBarPipe } from './filter-by-search-bar-pipe';

describe('FilterBySearchBarPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySearchBarPipe();
    expect(pipe).toBeTruthy();
  });
});
