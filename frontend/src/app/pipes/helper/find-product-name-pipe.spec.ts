import { FindProductNamePipe } from './find-product-name-pipe';

describe('FindProductNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FindProductNamePipe();
    expect(pipe).toBeTruthy();
  });
});
