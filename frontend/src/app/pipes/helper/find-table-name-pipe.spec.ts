import { FindTableNamePipe } from './find-table-name-pipe';

describe('FindTableNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FindTableNamePipe();
    expect(pipe).toBeTruthy();
  });
});
