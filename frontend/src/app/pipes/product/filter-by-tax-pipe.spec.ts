import { FilterByTaxPipe } from './filter-by-tax-pipe';

describe('FilterByTaxPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTaxPipe();
    expect(pipe).toBeTruthy();
  });
});
