import { FilterProductByFamilyPipe } from './filter-by-family-pipe';

describe('FilterProductByFamilyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductByFamilyPipe();
    expect(pipe).toBeTruthy();
  });
});
